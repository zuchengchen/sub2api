# czc-v2026.08.25.8（0.1.183.5）

API Key 的分组选择下拉与 Keys 表格分组徽章现在也显示 VIP **真实计费倍率**：`GET /groups/rates` 返回与计费同源的展示口径（管理员覆盖 ?? 分组默认，VIP 在减免分组叠加 -0.02；无覆盖也会输出减免分组条目）。前端沿用现有划线对比样式：~~0.17x~~ **0.15x**。

无迁移无配置变更；service/handler 全量测试通过。回滚换回上一版二进制即可。
