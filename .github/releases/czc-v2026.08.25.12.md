# czc-v2026.08.25.12（0.1.183.9）

VIP 在 gpt-pro 分组的倍率减免由 **−0.02 调整为 −0.05**（绝对差）。

以当前分组显示 0.17 为例：普通用户实付 0.17，SVIP 实付 **0.12**（约为标准价 70.6%）。减免下限仍为 0；管理员专属覆盖存在时按覆盖值减 0.05。

测试断言同步更新（0.15→0.10、覆盖 0.20→0.15、0.07→0.02 等）；无迁移无配置变更。
