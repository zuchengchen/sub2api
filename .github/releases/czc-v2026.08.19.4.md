# Sub2API 0.1.178.4（czc-v2026.08.19.4）

本版本移除内容风控对 GPT 前缀分组名的硬编码限制。审核范围改为完全服从管理员已保存的分组和模型过滤配置，非 GPT 分组不再被静默绕过。

| 项目 | 值 |
| --- | --- |
| 当前版本 | `0.1.178.4` |
| 版本基础 | 官方 `0.1.178` |
| 上一已部署自定义版本 | `czc-v2026.08.19.3` |
| 生产标识 | `czc-v2026.08.19.4` |
| 发布提交 | `b95a53bcf9322ad06a037f8cdf490b1a6854a4f7` |
| 完整对比 | [`czc-v2026.08.19.3...czc-v2026.08.19.4`](https://github.com/zuchengchen/sub2api/compare/czc-v2026.08.19.3...czc-v2026.08.19.4) |
| 在线二进制 SHA-256 | `6018ab3b75c078f0b2c7704f341ae9d341f937bd20d58acf994d132ac5aca9ed` |
| 安装时间 | `2026-08-19T08:49:16.775539848Z` |

## 变更

- API 密钥所属分组名仅作为审核元数据，不再要求名称以 `GPT` 开头；Claude、Gemini、Grok 等非 GPT 分组可以正常进入内容审核。
- `all_groups=true` 时覆盖全部分组；关闭后由 `group_ids` 精确控制审核分组。模型范围继续由 `model_filter` 的 `all`、`include` 或 `exclude` 控制。
- 不在配置范围内的请求不占用内容审核请求体容量；配置范围内的邮箱白名单请求仍进入 shadow 审核并使用容量预算。
- 上游严重 cyber-policy 事件不再因非 GPT 分组名而跳过日志和用户处置。

## 兼容性与升级

- 无数据库 schema 或配置格式迁移，保留现有配置、密钥、审核日志、归档和业务数据。
- 当前生产配置为全部分组、全部模型；升级后该配置会实际覆盖所有分组和模型。
- 回滚到 `0.1.178.3` 会恢复 GPT 分组名前缀限制，但不会修改数据库或配置。
- 本地版本号使用官方基础版本加数字修订号 `0.1.178.4`，不使用功能、代理、分支或供应商后缀。

## 发布验证

已通过：

- 本次部署请求之前，原修复工作树执行 `go test ./internal/service -count=1` 和 `go test ./internal/handler -count=1`，两个完整后端包均通过；按所有者要求，发布提交在部署前未重复执行测试。
- 部署后聚焦回归通过：分组名不再决定作用域、分组/模型配置过滤以及非 GPT cyber-policy 处置均符合预期。
- 部署后 `go test ./internal/handler -count=1` 通过（48.117 秒），更新过时验收断言后 `go test ./internal/service -count=1` 通过（117.779 秒）。
- 独立发布工作树只包含本次逻辑和回归用例；`git diff --check` 与 Vite 生产构建通过，合入 `dev-czc`、`main-czc` 及测试修正回合并均无冲突。

失败或跳过：

- 最初一次聚焦编译因编辑过程中遗漏仍被使用的 `unicode` 导入而失败；恢复导入后聚焦测试及上述完整包测试均通过。
- 首次候选构建因干净工作树尚无嵌入式 `frontend/dist` 而在生成二进制前停止；生成生产前端资源后重新构建成功，未触碰线上文件。
- 部署后首次完整 `service` 包运行发现两个旧验收场景仍断言“非 GPT 分组旁路”；运行代码的聚焦回归已通过，随后以纯测试提交 `07c8c52b0` 修正断言并重新跑完整包通过。
- 未执行真实外部供应商、OAuth、邮件、完整浏览器 E2E 或长时间 soak 测试，避免外部副作用并缩短本次修复发布时间。

## 部署与回滚

- 从 `main-czc` 的不可变发布提交构建，使用 `CGO_ENABLED=0`、`-tags embed`、`-trimpath` 和 `-buildvcs=false`，并注入版本、提交、构建时间和发布地址。
- 替换前归档实际在线 `0.1.178.3` 二进制，记录版本、提交、安装时间和 SHA-256；候选文件在 `/opt/sub2api` 同一文件系统暂存并原子替换，只重启主服务一次。
- 保持配置、数据库、业务数据、nginx 和独立 YuFeng 服务不变。部署后验证 systemd、`/health`、进程版本、在线 SHA-256 和回滚列表。
- 回滚 API/UI 只保留并展示两份通过 SHA-256 验证的本地归档；本次部署后为 `0.1.178.3` 和 `0.1.178.2`。

## 生产验证记录

- 候选从标签提交构建，报告版本 `0.1.178.4`、提交 `b95a53bcf9322ad06a037f8cdf490b1a6854a4f7`、构建时间 `2026-08-19T08:39:39Z` 和 Go `1.26.6`；候选、在线文件及 `/proc/<MainPID>/exe` SHA-256 均为 `6018ab3b75c078f0b2c7704f341ae9d341f937bd20d58acf994d132ac5aca9ed`。
- 同文件系统原子替换后只重启 `sub2api.service` 一次：服务于 `2026-08-19 16:49:22 CST` 进入 active，`Result=success`、`NRestarts=0`，`GET /health` 返回 HTTP 200 和 `{"status":"ok"}`。nginx 与独立 YuFeng 服务保持 active，未重启。
- 配置 SHA-256 在部署前后均为 `235dbea555f5a407bfec946b384e5dca7fd9ba9a148ab9d71de81095d6b80f74`；配置快照为 `/opt/sub2api/.version-history/config-snapshots/20260819T084914.630377156Z-235dbea555f5-0.1.178.4-config.yaml`。数据库无需迁移，当前运行设置核验为风控开启、`pre_block`、`all_groups=true`、模型过滤 `all`。
- 实际在线 `0.1.178.3` 已归档为 `20260819T084912.498741837Z-fa099220e8a1`，归档 SHA-256 为 `fa099220e8a1bed6049198bfb33d0dc511d564c68eb4ed133044efe1639aad52`。认证后的回滚 API 只返回该版本与已验证的 `0.1.178.2`；最旧的 `0.1.178.1` 已按两份上限清理。
