[Service]
Environment=GOMEMLIMIT=4GiB
Environment=CONFIG_FILE=/etc/sub2api/config.yaml
MemoryHigh=6G
MemoryMax=8G

# The application config referenced above must keep these deployment-owned
# paths outside the repository:
# content_moderation_archive.key_ring_path=/etc/sub2api/secrets/content-moderation-keyring.json
# content_moderation_archive.retry_dir=/var/lib/sub2api/content-moderation/retry
# content_moderation_archive.emergency_dir=/var/lib/sub2api/content-moderation/emergency
