location / {
    add_header Retry-After 30 always;
    default_type application/json;
    return 503 '{"error":{"type":"service_unavailable","code":"runtime_risk_cutover","message":"Service temporarily unavailable"}}';
}
