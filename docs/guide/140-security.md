## 保护账户和密钥

- 示例中的 `<你的 API Key>` 只是提示文字，必须换成自己的 Key，但不要把真实值发给别人。
- 下载 BAT 后，建议先核对 SHA-256 文件指纹。
- 不要运行群聊或陌生网站提供的修改版测速工具。
- 不要为了消除报错而关闭浏览器或 PowerShell 的安全检查。
- 备份文件只保存在自己的电脑上，不要上传。
- 联系管理员时，永远不要发送密码、完整 API Key、验证码或 Cookie（浏览器登录信息）。
